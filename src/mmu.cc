#include "MMU.h"

MMU::MMU() {

}

uint8_t MMU::read_byte(const Address address) const {
    return 0;
}

uint16_t MMU::read_word(const Address address) const {
    return 0;
}

void MMU::write_byte(const Address address, const uint8_t byte) const {

}

void MMU::write_word(const Address address, const uint16_t word) const {

}
